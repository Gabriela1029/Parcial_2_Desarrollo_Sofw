# Task App - Parcial 2 - Desarrollo de Software II

## Instalación
1. npm install
2. npm run dev
3. Visita http://localhost:3000

Endpoints: POST /tasks, GET /tasks, PATCH /tasks/:id/status, DELETE /tasks/:id, GET /tasks/overdue
